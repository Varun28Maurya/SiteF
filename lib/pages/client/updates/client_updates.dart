import 'package:flutter/material.dart';

class ClientUpdatesPage extends StatelessWidget {
  const ClientUpdatesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Client Updates"));
  }
}
