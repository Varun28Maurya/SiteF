import 'package:flutter/material.dart';

class ClientDocsPage extends StatelessWidget {
  const ClientDocsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Client Docs"));
  }
}
