import 'package:flutter/material.dart';

class ClientOverviewPage extends StatelessWidget {
  const ClientOverviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Client Overview"));
  }
}
